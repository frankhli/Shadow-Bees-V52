# Shadow-Bees V52 业务逻辑深度分析与系统重构计划

## 核心问题诊断

### 问题本质
当前系统的问题：**数据流是单向的，没有闭环**。修改了A，B和C不会自动更新。

### 理想的数据流
```
用户操作 → 状态变更 → 联动计算 → 全局更新 → UI刷新
    ↑                                      ↓
    └──────────── 反馈确认 ←───────────────┘
```

---

## 一、时间态三模式的真正业务逻辑

### 需求文档核心理解

#### 实时推演模式
**业务逻辑**: 系统是一个"活体"
- 时间在真实流动
- 事件随机发生（不是固定的）
- 成交随机产生
- 竞品价格会波动
- 库存实时减少
- **所有数据都在动态变化**

**当前问题**: 
- ✅ 成交有定时器生成
- ❌ 竞品价格不动
- ❌ 库存不动
- ❌ 定价建议不动
- ❌ 预警不动

#### 历史回放模式
**业务逻辑**: 观看"录影带"
- 加载一个固定的时间切片（快照）
- 可以播放、暂停、快进、拖拽
- 随着时间推进，数据按预定义序列变化
- **改变的是时间轴位置，不是数据本身**

**当前问题**:
- ✅ 有时间轴控制
- ❌ 时间轴变化时上面的数据不动
- ❌ 没有真正应用时间轴事件

#### 沙盘模拟模式
**业务逻辑**: "实验室"
- 冻结当前状态作为基准
- 手动调整变量（竞品价±20%、事件强度等）
- 系统实时计算响应结果
- **调整变量→即时看到影响**

**当前问题**:
- ✅ 有变量调整面板
- ❌ 调整变量时上面的数据不动
- ❌ 没有真正重新计算定价

---

## 二、十大联动的完整数据流

### 联动1: 酒店切换
**触发**: 用户选择新酒店
**影响链**:
```
酒店切换
  → 500ms Loading动画
  → 主题色渐变0.5s
  → 清空当前数据
  → 加载新酒店数据（竞品池、事件池、库存池）
  → 重置定价模式为酒店默认
  → 切换话术策略
  → 切换内容风格
  → 重置全局时间为today
  → 全页面数据刷新
```

**当前缺失**:
- ❌ 竞品数据不是动态加载的（写死）
- ❌ 事件数据不是动态加载的（写死）
- ❌ 话术策略没有真正切换
- ❌ 内容风格没有真正切换
- ❌ 时间没有重置

### 联动2: 房型切换
**触发**: 用户选择新房型
**影响链**:
```
房型切换
  → 骨架屏2秒
  → 重新加载该房型对标竞品
  → 重新计算历史均价
  → 重新计算市场均价
  → 重新评估优势标签（🟢🟡🟠🔴）
  → 内容工厂模板切换
  → 发布状态列表过滤为该房型
  → 定价滑块更新底价红线
```

**当前缺失**:
- ❌ 竞品没有按房型重新加载
- ❌ 历史均价没有重新计算
- ❌ 内容模板没有切换
- ❌ 发布列表没有过滤

### 联动3: 时间态切换
**触发**: 用户切换实时/历史/沙盘
**影响链**:
```
时间态切换
  → 清空当前模式数据
  → 加载新模式数据
  → 如果是实时：启动定时器
  → 如果是历史：加载快照
  → 如果是沙盘：冻结当前状态
  → 全页面数据刷新
```

**当前缺失**:
- ❌ 成交数据没有按模式切换
- ❌ 定价没有按模式切换
- ❌ 库存没有按模式切换

### 联动4: 定价修改
**触发**: 用户拖动价格滑块
**影响链**:
```
定价修改
  → 三平台价格按系数同步（<1秒）
  → 模式重判（黄牛>1.2倍/动态±15%/尾货<0.9倍）
  → 竞争力标签更新
  → 偏离度计算
  → 内容工厂价格同步
  → 偏离>15%触发预警
  → 审计记录插入
```

**当前缺失**:
- ❌ 内容工厂价格没有同步
- ❌ 审计记录没有真正插入

### 联动5: 模式切换
**触发**: 系统自动或手动切换定价模式
**影响链**:
```
模式切换
  → 主题色渐变0.5秒
  → 顶部横幅提示
  → 话术策略面板更新
  → AI客服回复风格切换
  → 内容工厂默认模板切换
  → 让价空间调整显示
```

**当前缺失**:
- ❌ 主题色渐变不明显
- ❌ AI风格没有真正切换（三种模式回复一样）
- ❌ 内容工厂模板没有切换

### 联动6: 成交确认
**触发**: 系统或用户确认成交
**影响链**:
```
成交确认
  → 库存扣减（动画进度条）
  → 财务流水插入
  → 战报数字滚动（number-roll动画）
  → 内容ROI更新
  → 顶部滚动条通知
  → 库存触阈值（<3间）触发模式重算建议
```

**当前缺失**:
- ❌ 库存动画进度条
- ❌ 数字滚动动画
- ❌ 内容ROI不更新
- ❌ 库存阈值不触发建议

### 联动7: 内容生成
**触发**: 用户点击生成内容
**影响链**:
```
内容生成
  → 库存预占15分钟（倒计时显示）
  → 根据当前模式选择话术策略
  → AI生成多版本（去AI化+高转化）
  → 确认发布
  → 占用平台配额（配额-1）
  → 发布状态添加记录
  → 售罄自动下架
```

**当前缺失**:
- ❌ 平台配额占用（没有配额系统）
- ⚠️ AI生成是固定模板，不是真正的多版本

### 联动8: 事件触发
**触发**: 事件强度变化或事件发生
**影响链**:
```
事件触发
  → 雷达图脉冲变化
  → 竞品价格波动
  → 定价建议调整
  → 模式可能自动切换
  → 预警区提示
```

**当前状态**: 基本已实现，但触发机制需要完善

### 联动9: 库存预警
**触发**: 库存<3间
**影响链**:
```
库存预警
  → 库存中心琥珀光晕脉冲
  → 今日概览预警
  → 定价建议提价
  → 内容工厂建议紧迫型内容
  → 系统建议关闭OTA释放灵活库存（需人工确认）
```

**当前缺失**:
- ❌ 定价建议没有自动提价
- ❌ 内容工厂没有建议紧迫型内容
- ❌ 没有OTA释放建议

### 联动10: 财务勾稽
**触发**: 任意成交或数据变化
**影响链**:
```
实时验证
  → 成交总金额 = Σ订单成交价
  → 平均成交价 = 总金额/成交量
  → 服务费 = 总金额×6%
  → 酒店实收 = 总金额-服务费
  → 验证公式显示✅
```

**当前状态**: 基本实现，但验证显示需要增强

---

## 三、系统重构设计

### 3.1 数据流架构

```
┌─────────────────────────────────────────────────────────────┐
│                     Global State                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │  Hotel   │ │ Pricing  │ │Inventory │ │Contents  │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ↑
                    ┌─────────┴─────────┐
                    │   TimeMode Store  │
                    │  ┌──────────────┐ │
                    │  │  Realtime    │ │
                    │  │  History     │ │
                    │  │  Sandbox     │ │
                    │  └──────────────┘ │
                    └───────────────────┘
                              ↑
                    ┌─────────┴─────────┐
                    │   User Actions    │
                    │  (switch/mode/...)│
                    └───────────────────┘
```

### 3.2 联动触发器设计

每个联动需要一个**触发器函数**，当数据变化时自动调用：

```typescript
// 联动触发器注册表
const linkages = {
  // 酒店切换触发
  onHotelSwitch: [updateCompetitors, updateEvents, updatePricing, updateInventory, ...],
  
  // 房型切换触发
  onRoomTypeSwitch: [updateCompetitorsForRoom, recalculateAvgPrice, updateAdvantageLabel, ...],
  
  // 时间态切换触发
  onTimeModeSwitch: [switchDataSource, restartSimulation, ...],
  
  // 定价修改触发
  onPricingChange: [updatePlatformPrices, recalculateMode, updateContentPrice, ...],
  
  // 成交触发
  onTransaction: [updateInventory, updateFinance, updateContentROI, ...],
  
  // 库存变化触发
  onInventoryChange: [checkThreshold, suggestModeChange, ...],
  
  // 事件触发
  onEventTrigger: [updateCompetitorPrices, suggestPricing, ...],
};
```

### 3.3 实时推演模式的完整实现

**定时器任务清单**（每3-7秒执行）：
1. 30%概率生成成交
2. 10%概率触发随机事件
3. 5%概率调整竞品价格（±5%）
4. 检查库存阈值
5. 重新计算定价建议
6. 更新所有关联UI

### 3.4 历史回放模式的完整实现

**时间轴播放逻辑**:
1. 加载快照的initialState到全局
2. 根据playbackPosition计算当前时间轴索引
3. 应用该索引之前的所有timeline事件到全局状态
4. 实时更新UI显示

### 3.5 沙盘模拟模式的完整实现

**变量调整响应逻辑**:
1. 冻结当前状态作为base
2. 用户调整变量
3. 基于base+variables重新计算：
   - 新竞品均价
   - 新建议定价
   - 新库存预测
   - 新成交预测
4. 显示对比（base vs simulated）

---

## 四、修复清单（按优先级）

### P0 - 核心数据流（必须修复）

1. **时间态三模式全局联动**
   - [ ] 实时模式：成交、定价、库存、竞品全部动态变化
   - [ ] 历史模式：时间轴驱动全局数据变化
   - [ ] 沙盘模式：变量调整即时影响全局数据

2. **酒店切换完整联动**
   - [ ] 竞品数据动态加载
   - [ ] 事件数据动态加载
   - [ ] 话术策略真正切换
   - [ ] 内容风格真正切换

3. **房型切换完整联动**
   - [ ] 按房型重新加载竞品
   - [ ] 重新计算历史/市场均价
   - [ ] 内容模板切换
   - [ ] 发布列表过滤

### P1 - 重要联动（强烈建议修复）

4. **定价修改联动**
   - [ ] 内容工厂价格同步
   - [ ] 审计日志真正记录

5. **成交确认联动**
   - [ ] 库存动画进度条
   - [ ] 数字滚动动画
   - [ ] 内容ROI更新
   - [ ] 库存阈值触发建议

6. **模式切换联动**
   - [ ] 主题色渐变动画
   - [ ] AI风格差异化回复
   - [ ] 内容工厂模板切换

### P2 - 体验优化（建议修复）

7. **库存预警联动**
   - [ ] 自动定价建议提价
   - [ ] 内容工厂紧迫型建议
   - [ ] OTA释放建议

8. **财务勾稽增强**
   - [ ] 实时验证显示
   - [ ] 异常预警

---

## 五、修复实施计划

### 阶段1: 重构时间态三模式全局联动（2小时）
- 修改 timeModeStore，添加全局状态联动
- 修改 TodayOverview，监听时间态变化
- 添加定时器任务：竞品波动、事件触发

### 阶段2: 重构酒店/房型切换（1.5小时）
- 创建动态数据加载机制
- 实现房型相关的竞品筛选
- 添加话术/风格切换逻辑

### 阶段3: 完善定价/成交/模式联动（1.5小时）
- 添加内容工厂价格同步
- 实现审计日志记录
- 添加动画效果

### 阶段4: 测试验证（1小时）
- 测试所有联动
- 验证数据一致性
- 修复边界情况

---

**分析完成时间**: 2026-02-11
**预计修复时间**: 6小时
**优先级**: P0 > P1 > P2
