# Shadow-Bees 企业版角色权限指南

## 角色定义

### 1. 集团管理员 (GROUP_ADMIN)
**定位**：系统最高权限，负责整个集团的配置和管理

**数据范围**：
- 可查看所有酒店（1000+家）
- 可查看所有区域数据

**核心权限**：
- 全部查看权限（数据看板、订单、内容、客服等）
- 全部操作权限（定价、库存、订单处理、内容发布等）
- 用户管理（创建/编辑/删除用户）
- 账号分配（分配渠道账号给酒店）
- 系统设置
- 审计日志查看

**界面特点**：
- 显示酒店选择器，可选择多家酒店进行批量操作
- 显示"门店对比"等集团级功能

---

### 2. 集团运营 (GROUP_OPERATOR)
**定位**：日常运营人员，负责集团层面的运营工作

**数据范围**：
- 可查看所有酒店（1000+家）
- 可查看所有区域数据

**核心权限**：
- 全部查看权限（与管理员相同）
- 日常操作权限（定价调整、库存管理、订单处理、内容发布、客服回复等）
- **不包含**：用户管理、账号分配、系统设置

**与管理员的主要区别**：
| 功能 | 管理员 | 运营 |
|------|--------|------|
| 查看数据 | ✅ | ✅ |
| 日常操作 | ✅ | ✅ |
| 用户管理 | ✅ | ❌ |
| 账号分配 | ✅ | ❌ |
| 系统设置 | ✅ | ❌ |
| 查看审计日志 | ✅ | ❌ |

**界面特点**：
- 显示酒店选择器，可选择多家酒店
- 不显示用户管理、系统设置等菜单

---

### 3. 酒店店长 (HOTEL_MANAGER)
**定位**：单体酒店管理者，负责本酒店的日常运营

**数据范围**：
- **仅可查看自己管理的酒店**
- 不能查看其他酒店数据

**核心权限**：
- 单酒店全部查看权限
- 单酒店全部操作权限（定价、库存、订单、内容、客服）
- **不能**进行跨酒店操作
- **不能**查看集团级数据（如门店对比）

**界面特点**：
- **不显示酒店选择器**（自动选中自己的酒店）
- 顶部显示"当前酒店：XXX"而不是"数据范围"
- 实时成交列表只显示房型，不显示酒店名（因为只有一家酒店）
- "酒店概况"改为"酒店信息"，不显示"X家"标识
- 不显示"查看门店对比"按钮

---

## 角色选择建议

| 场景 | 推荐角色 |
|------|---------|
| 集团高管查看整体数据 | 集团管理员 |
| 集团运营人员进行日常调价、库存管理 | 集团运营 |
| 管理整个集团的账号分配 | 集团管理员 |
| 单体酒店店长管理自己的酒店 | 酒店店长 |
| 区域经理管理区域内多家酒店 | 区域经理 (REGION_MANAGER) |

## 技术实现说明

### 权限判断方法
```typescript
// 判断是否集团视角（显示酒店选择器）
const isGroupPerspective = user.dataScope === 'all' || user.dataScope === 'region';

// 判断是否单酒店视角
const isSingleHotel = user.dataScope === 'hotel';
```

### 酒店数据初始化
```typescript
// 集团视角：默认选中前50家酒店
if (isGroupLevel) {
  selectMultipleHotels(hotels.slice(0, 50).map(h => h.id));
} 
// 单酒店视角：只选中自己有权限的酒店
else {
  selectMultipleHotels(user.hotelIds);
}
```

### UI适配
- 集团视角：显示酒店选择器，支持多选
- 单酒店视角：隐藏酒店选择器，自动选中唯一酒店，简化界面
